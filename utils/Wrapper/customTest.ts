// import { test as base, expect } from '@playwright/test';
// import { allure } from 'allure-playwright';

// export const test = base.extend({});

// test.beforeEach(async (fixtures, testInfo) => {
//   // fixtures contains page, context, etc.
//   // testInfo contains test metadata like title
//   const tags = testInfo.title.match(/@(\w+)/g);
//   if (tags) {
//     tags.forEach(tag => allure.label({ name: 'tag', value: tag.replace('@', '') }));
//   }
// });

// export { expect };
